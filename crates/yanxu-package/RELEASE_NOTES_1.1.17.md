# yanxu-package 1.1.17

本版本与言序 1.1.17 同步发布，保持清单格式 2、锁文件格式 2 与公开
Rust API 兼容。

- `PermissionSet` 支持 IPv4 与 IPv6 CIDR 网络授权，并在宿主/应用求交集时保留更窄范围。
- DNS 解析后的敏感地址复核接受显式 CIDR，通配符与本地网络能力的既有边界不变。
- 原生宿主事件泵的嵌套请求改为成功 no-op，避免回调把外层 GUI 调用错误化。
- 包版本继续与核心版本同步，使诊断、锁文件生成器和发布元数据保持一致。
- 既有格式 1/2 清单和锁文件无需迁移。
