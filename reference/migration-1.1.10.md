# 从言序 1.1.9 迁移到 1.1.10

1.1.10 是 HTTP 边界修复版，不改变语言规范、标准库函数签名、原生 ABI、
清单、锁文件、YXB 或 Bundle 格式。

`网络.请求`与`网络.请求字节`现在对 4xx 和 5xx 返回响应典。以前捕获`NET_STATUS`的
调用方应改为读取`状态`，并按业务需求处理首部与正文。TLS 证书校验失败现在
稳定返回`NET_TLS`。
