# 迁移到言序 1.1.14

1.1.14 修复`网络.请求字节`将最大响应字节错误限制为套接字 4 MiB 单次接收上限的问题。

- 使用不大于 4 MiB 预算的应用无需修改。
- 使用更大预算的应用需升级到 1.1.14 并重新构建 YXB 或 Bundle。
- 实际响应超过调用方预算时仍以`NET_LIMIT`失败，不返回截断正文。
- 1.1.16 起单个字节值与默认宿主 HTTP 上限统一为 16 MiB；应用请求与宿主上限取较小者，套接字单次读取仍独立限制为 4 MiB。

本版本不改变标准库签名、原生 ABI、清单、锁文件、YXB 或 Bundle 格式。
