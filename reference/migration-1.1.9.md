# 从言序 1.1.8 迁移到 1.1.9

1.1.9 是兼容性修复版本，不改变语言规范 1、标准库 API、原生 ABI v1/v2、清单与锁文件、
YXB 或 Bundle 格式。

命令行现在可用`--max-steps <N>`或`YANXU_MAX_STEPS`调整执行步数预算。旗标优先于环境变量，
两者都未设置时仍使用默认值 1000000。常驻程序的每个宿主事件回调独立使用该预算，多个正常
回调不再因累计步数被误判为失控；单个回调中的死循环仍会被拦截。

GUI 与原生扩展应用只需升级运行时并重新构建，无需修改源码或重新制作原生扩展。
