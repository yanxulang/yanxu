# 迁移到言序 1.1.15

1.1.15 为需要访问用户配置局域网服务的应用新增显式本地网络能力。

- `网络 = ["*"]`仍只表示任意公共网络主机，不会隐式授权敏感地址。
- 需要同时访问公网和本地服务的应用可在`[权限]`中声明
  `网络 = ["*"]`与`本地网络 = true`。
- 本地网络能力覆盖回环、RFC1918、IPv4 链路本地、CGNAT、IPv6 ULA
  与 IPv6 链路本地；其他特殊地址仍须精确 IP 授权。
- 嵌入宿主与应用必须同时授予本地网络能力，最终权限交集才会生效。
