引「标准:套接字」为 套接字；

定 监听器：套接字 为 套接字.TCP监听（「127.0.0.1:0」）；
言 类型（监听器）；
言 监听器 是 套接字；
言 套接字.对端地址（监听器）；
套接字.关闭（监听器）；

定 数据报：套接字 为 套接字.UDP绑定（「127.0.0.1:0」）；
言 类型（数据报）；
套接字.关闭（数据报）；

试 则
    套接字.本地地址（数据报）；
救 所误 则
    言 所误.代码；
    言 所误.类别；
终
