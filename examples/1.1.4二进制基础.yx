引「标准:字节」为 字节；
引「标准:哈希」为 哈希；
引「标准:随机」为 随机；
引「标准:日期」为 日期；
引「标准:环境」为 环境；

定 正文：字节串 为 字节.从文字（「言序🌏」）；
言 字节.长度（正文）；
言 字节.转数列（字节.切片（正文，0，6））；

定 密钥：字节串 为 随机.安全字节（32）；
定 签名：字节串 为 哈希.HMACSHA256（密钥，正文）；
言 字节.长度（签名）；
言 哈希.恒时相等（签名，签名）；

言 日期.HTTP日期（0）；
言 环境.参数（）；
