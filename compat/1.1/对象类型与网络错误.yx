类 根 则
    公 域 名：文；
    法 初始化（名：文）则 置 此.名 为 名；终
    法 名号（）：文 则 归 此.名；终
终

类 枝 承 根 则
    法 初始化（名：文）则 父.初始化（名）；终
    法 名号（）：文 则 归 父.名号（） 加 「枝」；终
终

定 值：根 为 枝（「言序」）；
言 值.名号（）；
言 值 是 枝；
言 值 是 根；

引「标准:网络」为 网络；
试 则
    网络.请求（「GET」，「ftp://example.com」，「」，100，1024）；
救 所误 则
    言 所误.代码；
    言 所误.类别；
终
